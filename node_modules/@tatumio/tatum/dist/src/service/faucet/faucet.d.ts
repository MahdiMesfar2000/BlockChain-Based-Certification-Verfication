import { ResponseDto } from '../../util';
import { TxIdResponse } from './faucet.dto';
export declare class Faucet {
    private readonly id;
    private readonly connector;
    private readonly config;
    private readonly logger;
    constructor(id: string);
    fund(address: string): Promise<ResponseDto<TxIdResponse>>;
    private convertToFaucetChain;
}
